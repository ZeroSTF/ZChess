package tn.zeros.zchess.engine.models;

import tn.zeros.zchess.core.model.BoardState;

public class MinMaxModel implements EngineModel {
    @Override
    public int generateMove(BoardState boardState) {
        return 0;
    }
}
